services:
  - type: web
    name: crm-backend
    env: node
    plan: free
    branch: master
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      # Do not set PORT — Render injects PORT for web services.